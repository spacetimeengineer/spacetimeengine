#!/usr/bin/env python
from sympy import *

"""
Each of these functions require:
1.) The equations whichgenerate them.
2.) Will then need to parse the unkown constants for user input.
3.) Those constants will need to be defined with physical units.
"""


def plot_four_accelleration_paths():
    textplot(sin(t)*t, 0, 15)
    return plot

def plot_four_velocity():
    textplot(sin(t)*t, 0, 15)
    return plot

def plot_four_position():
    textplot(sin(t)*t, 0, 15)
    return plot

def plot_four_jerk():
    textplot(sin(t)*t, 0, 15)
    return plot

